def add_numbers(a, b):
    print(a + b)  # ❌ Missing 'return' statement

def greet(name)
    print("Hello, " + name)  # ❌ Missing colon in function definition

numbers = [1, 2, 3, 4, 5]
for i in range(len(numbers)):  # ❌ Incorrect loop variable usage
print(numbers[i])  # ❌ Indentation error

add_numbers(5, 3)
greet("hallema")
